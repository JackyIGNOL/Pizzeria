document.addEventListener("DOMContentLoaded", function () {
  
    var tbbutton = document.querySelectorAll(".menuHCell");

    var tbframe = document.querySelectorAll(".menuMain");

    tbbutton.forEach(element => {
        console.log(element);
    });
    tbframe.forEach(element => {
        console.log(element);
    });

    /* buttonA.addEventListener("click", function () {
    }); */
})